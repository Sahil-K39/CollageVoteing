services:
  - type: web
    name: collage-voting-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn blockchain_voting.main:app --host 0.0.0.0 --port $PORT
